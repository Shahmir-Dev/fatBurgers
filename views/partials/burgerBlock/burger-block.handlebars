<li>
    {{burger_name}}

    <button class="change-devour" data-id="{{id}}" data-new="{{devoured}}">
        {{#if dev}}!{{else}}devour!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>